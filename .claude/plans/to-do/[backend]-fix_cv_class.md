# [backend] fix & optimise the CV class

**Roadmap item 1.** Make `backend/jac/cv.py` rank/filter career entries by fit to a job posting
via the `light` / `standard` / `strong` AI ladder. `light` (embedding rank) is the working floor;
`standard` (instruction LLM) and `strong` (conversational LLM) rungs are stubs to flesh out.

> Placeholder backlog item. Run `/setup-guide` to turn this into a code-bearing implementation guide.
