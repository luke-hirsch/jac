# [frontend] CRUD API calls for the ResumeSnippet model

**Supports roadmap items 2–3.** Frontend CRUD wiring for `ResumeSnippet` so the user can manage
cover-letter boilerplate, ahead of the tailored CV + cover-letter render.

> Placeholder backlog item. Run `/setup-guide` to turn this into a code-bearing implementation guide.
