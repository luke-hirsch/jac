# [backend] cover-letter / resume creation pipeline

**Roadmap item 2.** A class that writes a cover letter for a job, stitching `ResumeSnippet`
boilerplate with the LLM writer model (`llama3.2:1b`) to keep it personal and avoid AI slop.

> Placeholder backlog item. Run `/setup-guide` to turn this into a code-bearing implementation guide.
