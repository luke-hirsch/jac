# Django
celery
channels
daphne
Django[argon2]      # the extra pulls argon2-cffi — see PASSWORD_HASHERS in settings.py
django-allauth[mfa]
django-cors-headers
django-filter
djangorestframework
drf-spectacular
Pillow
redis
qdrant-client

# LLM provider SDKs — install only what you use
anthropic           # provider: anthropic
openai              # provider: openai  or  provider: custom (Ollama etc.)
google-generativeai  # provider: google
